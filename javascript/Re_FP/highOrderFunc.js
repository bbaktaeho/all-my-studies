/**
 * 일급
 * * 값으로 다룰 수 있음
 * * 변수에 담을 수 있음
 * * 함수의 인자로 사용될 수 있음
 * * 함수의 결과로 사용될 수 있음
 */

/**
 * 일급 함수
 * * 함수가 값으로 다뤄지는 함수
 */

/**
 * 고차 함수
 * * 함수를 인자 값으로 다루는 함수
 * * 클로저를 만들어 리턴하는 함수
 */