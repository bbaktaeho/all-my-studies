import { SwaggerConfig } from './sagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Nest js Template',
  description: 'Template',
  version: '1.0',
  tags: ['task'],
};
